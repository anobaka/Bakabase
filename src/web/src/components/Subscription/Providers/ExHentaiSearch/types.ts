/**
 * Mirrors the backend ExHentaiSearchTarget record. Keep field names in sync with C#
 * (camelCase via the default JSON naming policy).
 */
export interface ExHentaiSearchTarget {
  url: string;
}
