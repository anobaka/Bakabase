<Application
    x:Class="Bakabase.Shell.App"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    RequestedThemeVariant="Light">
    <Application.Styles>
        <FluentTheme />
    </Application.Styles>

    <TrayIcon.Icons>
        <TrayIcons>
            <!--
              Hidden until App.OnFrameworkInitializationCompleted has wired its menu: an icon
              whose Open and Exit do nothing is worse than none, and a launch the
              single-instance guard refuses never gets this far at all.
            -->
            <TrayIcon Icon="avares://Bakabase.Shell/Assets/favicon.ico"
                      ToolTipText="Bakabase"
                      IsVisible="False">
                <TrayIcon.Menu>
                    <!--
                      Headers double as lookup keys in App.OnFrameworkInitializationCompleted.
                      TrayMenuController then localises them and, when the host can switch the
                      main window between servers, inserts a "Switch to" submenu after Open.
                    -->
                    <NativeMenu>
                        <NativeMenuItem Header="Open" />
                        <NativeMenuItem Header="Exit" />
                    </NativeMenu>
                </TrayIcon.Menu>
            </TrayIcon>
        </TrayIcons>
    </TrayIcon.Icons>
</Application>
