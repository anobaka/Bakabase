[git]
conventional_commits = false
filter_unconventional = false
topo_order = false
protect_breaking_commits = true
link_parsers = [
  { pattern = "#(\\d+)", href = "https://github.com/anobaka/Bakabase/issues/$1"}
]
filter_commits = false

commit_parsers = [
  { message = "^feat", group = "Features" },
  { message = "^fix", group = "Bug Fixes" },
  { message = "^perf", group = "Performance" },
  { message = "^refactor", group = "Refactoring" },
  { message = "^docs", group = "Documentation" },
  { message = "^style", group = "Styling", skip = true },
  { message = "^test", group = "Testing", skip = true },
  { message = "^build", group = "Build System", skip = true },
  { message = "^ci", group = "CI", skip = true },
  { message = "^chore", group = "Chores", skip = true },
  { message = "^revert", group = "Reverts" },
]

# [remote.github]
# owner = "anobaka"
# repo  = "Bakabase"
# token = "GITHUB_TOKEN"

[changelog]
header = ""
body = """
{%- for group, commits in commits | group_by(attribute="group") -%}
### {{ group }}
{% for commit in commits %}
- {{ commit.message | upper_first }} ({{ commit.id | truncate(length=7) }})
  {%- if commit.extra is defined and commit.extra.issues is defined -%}
    {% for issue in commit.extra.issues %}
  - [#{{ issue.number }}]({{ issue.url }}) {{ issue.title }}
    {%- endfor -%}
  {% endif %}
{% endfor %}
{% endfor %}
{% if version %}

### 下载地址

- 阿里云CDN: https://cdn-public.anobaka.com/app/bakabase/inside-world/{{ version | trim }}/installer/bakabase.zip
- Github: 通过附件下载对应版本的压缩包即可。
{% endif %}
"""
footer = ""
trim = false